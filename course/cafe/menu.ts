/**
 * The café. Same one from the web page, so the domain costs you nothing.
 *
 * Re-exported from the site's own module rather than copied. The Lab in your
 * browser and this course grade against the *same* menu — if they drifted,
 * your eval score here would not mean what it means there. One source of
 * truth is not tidiness; it is the reason the numbers are comparable.
 */
export { MENU, NON_COFFEE, menuText, priceOf } from "../../lib/cafe/menu";
export type { Order, OrderItem, Size } from "../../lib/cafe/menu";
