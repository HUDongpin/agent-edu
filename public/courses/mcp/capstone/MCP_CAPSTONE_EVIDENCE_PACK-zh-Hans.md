# MCP 课程 10 — 顶点证据包

> 该版本完全翻译自英文源，并通过了自动结构和技术术语检查。没有声称人类语言审查。

协议基线：MCP `2026-07-28`<br>
课程评估版本：`2026-07-28-v2`<br>
课程证据快照：`2026-08-24`

该模板支持构建者轨道（实现小型服务器和客户端）或审计者轨道（复制和审查公共服务器）。完成它是自我证明，而不是独立验证的证书。共享之前删除所有秘密和私人数据。

## 1. 适配决策

- 用户和工作：
- 为什么使用 MCP 而不是直接的 API 或普通函数：
- 最小能力表面：
- 明确的非目标：
- 权威人士介绍：

## 2. 架构和数据流

附上一张图表，显示用户、模型、主机、每台服务器一个 MCP 客户端、服务器、上游系统、传输、凭据、日志和存储的状态。对于每条边，说明哪些数据穿过它以及谁可以保留它。

## 3. 版本清单

| 物品 | 精确版本或不可变修订版 | 来源 | 验证日期 |
| --- | --- | --- | --- |
| MCP 协议 | 2026-07-28 |  |  |
| SDK |  |  |  |
| 服务器 |  |  |  |
| 主机/客户端 |  |  |  |
| 运行时和锁文件 |  |  |  |

## 4. 能力合同

对于每个工具、资源、提示、启发和协商扩展，记录：

- 名称或 URI；
- 交互所有者；
- 输入和输出模式；
- 授权和批准规则；
- 正常、空、无效、未授权、冲突、超时和上游故障行为；
- 返回的证据；
- 回滚或写入补偿。

## 5. 直接协议证据

- `server/discover` request/response 以及当前每个请求的元数据和 `resultType`；
- 原始 list/read/get/call 迹线；
- 正常和预期故障痕迹；
- 修订日志；
- 对于仅作为历史证据保留的任何旧版 Inspector UI 的注释。

## 6. 主机集成证据

- 主机名和确切版本；
- 配置的传输和服务器身份；
- 协商的协议和功能；
- 有效的工具许可名单和批准政策；
- 一个成功的只读工作流程；
- 一种被拒绝或预期失败的工作流程。

## 7. 威胁模型和对抗性测试

运行下面所有 12 个命名案例。如果一行组合了相关变体，则练习每个变体并在该行中保留单独的观察结果。

| 案件 | 资产或边界 | 强制控制 | 预期信号 | 观察结果 |
| --- | --- | --- | --- | --- |
| 1 | 提示或结果注入 | 将返回的内容保存在不受信任的数据通道中 | 注入不能改变更高优先级的政策 |  |
| 2 | 恶意注释或隐藏指令 | 将注释视为提示；检查内容块 | 没有权威或政策提升 |  |
| 3 | 路径遍历 | 规范化并限制允许的路径 | 超出范围的路径被拒绝 |  |
| 4 | 超大内容 | 强制执行字节、项目和上下文限制 | 有界拒绝或安全截断信号 |  |
| 5 | 架构绕过或未知字段 | 验证 JSON 架构并拒绝额外内容 | 确定性无效参数结果 |  |
| 6 | 错误的受众或令牌传递 | 验证受众；从不向上游转发客户端令牌 | 请求被拒绝且令牌未泄漏 |  |
| 7 | 重定向、SSRF 或 DNS 重新绑定 | 将目的地列入白名单并重新验证每个跃点 | 内部或不允许的目标被阻止 |  |
| 8 | 状态句柄重放 | 使用不可预测的句柄并重新授权每个请求 | 跨用户或过期重播被拒绝 |  |
| 9 | 重复写入 | 幂等性或精确修订保护 | 最多一项预期的改变 |  |
| 10 | 取消比赛 | 合作取消加上取消后状态检查 | 无隐藏的后期副作用 |  |
| 11 | 包或端点妥协 | 固定不可变的出处并执行禁用路径 | 可以隔离受损的集成 |  |
| 12 | 上行超时 | 截止日期、有限重试和清晰的错误映射 | 没有无限挂起或重复效果 |  |

## 8. 来源和数据

对于每个事实声明或重复使用的数字，记录标题、发布者、直接 URL、可能的精确修订、access/observation 日期、证据层、重复使用基础及其支持的有界声明。

## 9. 评估和限制

分别报告连接、发现、选择、参数有效性、执行、授权、用户拒绝、取消和端到端任务结果。说明样本、环境、缺失的证据和已知的局限性。

## 10. 禁用和恢复演练

- 禁用一台服务器或工具；
- 撤销其凭证；
- 从经过编辑的日志中识别其最近的操作；
- 恢复已知良好的配置；
- 验证旧服务器无法运行；
- 记录时间、负责人、故障以及后续工作。

## 审稿人签字（可选）

- 审稿人：
- 审核日期：
- 检查的证据：
- 复制安全路径：
- 重现预期失败：
- 需要更正：
- 决定和范围：
