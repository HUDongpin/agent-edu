import { PRODUCTION_AI_COURSE } from "./definition";

/** Canonical, validator-checked manifest derived by the shared course kit. */
export const PRODUCTION_AI_MANIFEST = PRODUCTION_AI_COURSE.manifest;
