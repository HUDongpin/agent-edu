import { DEEP_LEARNING_COURSE } from "./definition";

/** Canonical, validator-checked manifest derived by the shared course kit. */
export const DEEP_LEARNING_MANIFEST = DEEP_LEARNING_COURSE.manifest;
